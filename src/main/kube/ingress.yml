apiVersion: extensions/v1beta1
kind: Ingress
metadata:
  namespace: testunited
  name: core
  annotations:
    nginx.ingress.kubernetes.io/use-regex: "true"
spec:
  rules:
  - host: testunited.sandbox.gke
    http:
      paths:
      - path: /*
        backend:
          serviceName: core
          servicePort: 80